extends layout

block content
    h1.
        User List
    block link
       -selected="Users"
    ul
        each user, i in userlist
            li
                a(href="mailto:#{user.email}")= user.username